# PyPI Download Statistics

_Auto-generated by GitHub Actions on 2026-03-18 17:51 UTC._

## Package
- `qmcpy`

## Summary
- Downloads in the last month reported by `pypistats recent`: **547**
- Total downloads across the daily time series returned by `pypistats overall` with mirrors: **29,634**

## Notes
- The PyPI Stats API returns `recent` data as a dictionary with keys such as `last_day`, `last_week`, and `last_month`.
- The `overall` endpoint returns a daily time series list with per-day download counts. These two summaries are not interchangeable.
- PyPI Stats notes that download data is updated once daily and recent time-series data is retained for 180 days.

## Reproduce locally
```bash
python -c "import pypistats; print(pypistats.recent('qmcpy', format='json'))"
python -c "import pypistats; print(pypistats.overall('qmcpy', mirrors='with', format='json'))"
```
