version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.9"
  jobs:
    post_install:
      - "pip install -e ."
      - make _uml
      - make _doc

conda:
  environment: environment.yml

formats: all