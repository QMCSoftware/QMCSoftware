name: Docker Image CI

on:
  push:
    branches: [ "choidocker" ]
  pull_request:
    branches: [ "choidocker" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag qmcpy:$(date +%s)
